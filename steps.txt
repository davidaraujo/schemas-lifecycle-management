git checkout -b new-branch-schemas-management

change account.json schema and add a new field
```
    "mobile_enabled": {
      "type": "boolean",
      "description": "VIP flag for mobile app"
    }
```

git add .
git commit -m "New field on schema xpto ...."
// push to remote
git push origin new-branch-schemas-management





